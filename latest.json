{
  "version": "0.5.1",
  "notes": "A corrupt model cache now repairs itself. A load that fails without downloading anything means the file on disk is bad — truncated by a dropped connection — and it used to fail identically forever, because nothing re-fetches a file that is already there. It is now cleared and refetched once. Also: when a take is served by the bundled starter model while a larger tier downloads, the capsule says so instead of quietly appearing to get worse.",
  "downloads": {
    "win32": "https://github.com/letssay-ANONYMOUS/utter-releases/releases/download/v0.5.1/Utter-Windows-x64-0.5.1.exe",
    "linux": "https://github.com/letssay-ANONYMOUS/utter-releases/releases/download/v0.5.1/Utter-Linux-x64-0.5.1.AppImage"
  }
}
